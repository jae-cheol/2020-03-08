1)	* 1단계 : 드라이버 로딩 - 동적으로 클래스 인식하게 함.
	 * 2단계 : 드라이버 선택, DB접속 후 연결객체 얻기 - 접속하기 위한 ip,port등등.. 알아야됨.
	 * 3단계 : SQL문 실행 - sql 실행 객체 얻기 및 실행
	 * 4단계 : 결과 얻기
	 * 5단계 : DB접속 해제 - 연결해제 - finally에 있어야함.
	 
2) 컬럼에 alias쓰면 그 이후부턴 변경된 alias로 써줘야함.

3) 외부(ip) db가 접속하게 하려면 바꿔줘야하는 파일이 있음. 아직 하진 않음.